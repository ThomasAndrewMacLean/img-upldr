<!DOCTYPE html>
html(lang="en")
    include includes/head.pug
    body(style="opacity:0;background:red")
        nav
            a(href="pics") pics
        main(id="page")
            form(id="form-file", class="center", action="")
                input(type="file",name="image", id="imgupload")
                label(for="imgupload" id="imgupload-btn") upload image
            canvas(id="canvas", height="400", width="400", class="center")

            include includes/footer.pug